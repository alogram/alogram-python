# coding: utf-8

"""
Payments Risk API

API for detecting and scoring fraud for purchases, with lifecycle labeling and behavioral signals. v1 focuses on purchases only (`/risk/check`), with future account/session and KYC checks stubbed below.

The version of the OpenAPI document: 0.1.6-rc.3
Contact: support@alogram.ai
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing_extensions import Annotated, Self


class EntityIds(BaseModel):
    """
    Canonical entity identifiers (tenant/client/user/device/session). Do not send raw PII. Hash emails/phones/addresses as sha256 and prefix with 'sha256_'. Tiered support:   - tenantId is the top-level SaaS customer (required for all flows).   - clientId is the tenant's downstream business customer (e.g. merchant/partner) and MAY be omitted for     processor / bank / PSP-style integrations.   - endCustomerId is the tenant/client's consumer or business account (cardholder, shopper, etc.) and     MAY be omitted for purely device- or instrument-level checks.
    """  # noqa: E501

    tenant_id: Optional[
        Annotated[str, Field(min_length=5, strict=True, max_length=64)]
    ] = Field(
        default=None,
        description='Canonical ID for the paying organization (Tenant). Opaque, immutable, lowercase.  Must start with "tid_". Do not use domains or emails here. For a tenant\'s domain,  use a separate field (e.g., tenantDomain). ',
        alias="tenantId",
    )
    client_id: Optional[
        Annotated[str, Field(min_length=5, strict=True, max_length=100)]
    ] = Field(
        default=None,
        description="Canonical ID for the Tenant’s business customer (e.g., merchant/partner).  Opaque, immutable, lowercase. Must start with 'cid_'. ",
        alias="clientId",
    )
    end_customer_id: Optional[
        Annotated[str, Field(min_length=6, strict=True, max_length=100)]
    ] = Field(
        default=None,
        description='Canonical ID for the client’s end user / consumer (account holder).  Opaque, immutable, lowercase. Must start with "ecid_".  Do not put PII (like emails or phone numbers) in this field. ',
        alias="endCustomerId",
    )
    member_id: Optional[
        Annotated[str, Field(min_length=5, strict=True, max_length=100)]
    ] = Field(
        default=None,
        description="Canonical ID for a Tenant member/operator (employee/contractor) using the platform.  Opaque, immutable, lowercase. Must start with 'mid_'. ",
        alias="memberId",
    )
    payment_instrument_id: Optional[
        Annotated[str, Field(strict=True, max_length=100)]
    ] = Field(
        default=None,
        description="Tokenized instrument ID (non-PCI).",
        alias="paymentInstrumentId",
    )
    device_id: Optional[Annotated[str, Field(strict=True, max_length=100)]] = Field(
        default=None,
        description="Server-issued stable device token (device-level identifier). Should persist across sessions and logins on the same browser/device. ",
        alias="deviceId",
    )
    session_id: Optional[Annotated[str, Field(strict=True, max_length=100)]] = Field(
        default=None,
        description="Application/user session identifier (login or checkout session). Typically rotates more frequently than deviceId and may be tied to authentication. ",
        alias="sessionId",
    )
    email_hash: Optional[Annotated[str, Field(strict=True, max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased email hash (e.g., sha256).",
        alias="emailHash",
    )
    email_domain_hash: Optional[Annotated[str, Field(strict=True, max_length=71)]] = (
        Field(
            default=None,
            description="Normalized+lowercased email *domain* hash (e.g., sha256).",
            alias="emailDomainHash",
        )
    )
    phone_hash: Optional[Annotated[str, Field(strict=True, max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased phone hash (e.g., sha256).",
        alias="phoneHash",
    )
    shipping_address_hash: Optional[
        Annotated[str, Field(strict=True, max_length=71)]
    ] = Field(
        default=None,
        description="Normalized+lowercased shipping address hash (e.g., sha256).",
        alias="shippingAddressHash",
    )
    billing_address_hash: Optional[
        Annotated[str, Field(strict=True, max_length=71)]
    ] = Field(
        default=None,
        description="Normalized+lowercased billing address hash (e.g., sha256).",
        alias="billingAddressHash",
    )
    __properties: ClassVar[List[str]] = [
        "tenantId",
        "clientId",
        "endCustomerId",
        "memberId",
        "paymentInstrumentId",
        "deviceId",
        "sessionId",
        "emailHash",
        "emailDomainHash",
        "phoneHash",
        "shippingAddressHash",
        "billingAddressHash",
    ]

    @field_validator("tenant_id")
    def tenant_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^tid_[a-z0-9_-]{2,60}$", value):
            raise ValueError(
                r"must validate the regular expression /^tid_[a-z0-9_-]{2,60}$/"
            )
        return value

    @field_validator("client_id")
    def client_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^cid_[a-z0-9_-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^cid_[a-z0-9_-]{2,96}$/"
            )
        return value

    @field_validator("end_customer_id")
    def end_customer_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^ecid_[a-z0-9_-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^ecid_[a-z0-9_-]{2,96}$/"
            )
        return value

    @field_validator("member_id")
    def member_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^mid_[a-z0-9_-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^mid_[a-z0-9_-]{2,96}$/"
            )
        return value

    @field_validator("payment_instrument_id")
    def payment_instrument_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z0-9_:-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^[a-zA-Z0-9_:-]{2,96}$/"
            )
        return value

    @field_validator("device_id")
    def device_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^did_[a-zA-Z0-9_:-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^did_[a-zA-Z0-9_:-]{2,96}$/"
            )
        return value

    @field_validator("session_id")
    def session_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sid_[a-zA-Z0-9_:-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^sid_[a-zA-Z0-9_:-]{2,96}$/"
            )
        return value

    @field_validator("email_hash")
    def email_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("email_domain_hash")
    def email_domain_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("phone_hash")
    def phone_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("shipping_address_hash")
    def shipping_address_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("billing_address_hash")
    def billing_address_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EntityIds from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EntityIds from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError(
                    "Error due to additional fields (not defined in EntityIds) in the input: "
                    + _key
                )

        _obj = cls.model_validate(
            {
                "tenantId": obj.get("tenantId"),
                "clientId": obj.get("clientId"),
                "endCustomerId": obj.get("endCustomerId"),
                "memberId": obj.get("memberId"),
                "paymentInstrumentId": obj.get("paymentInstrumentId"),
                "deviceId": obj.get("deviceId"),
                "sessionId": obj.get("sessionId"),
                "emailHash": obj.get("emailHash"),
                "emailDomainHash": obj.get("emailDomainHash"),
                "phoneHash": obj.get("phoneHash"),
                "shippingAddressHash": obj.get("shippingAddressHash"),
                "billingAddressHash": obj.get("billingAddressHash"),
            }
        )
        return _obj
